# Cost of Carry Dashboard Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cost_of_carry_db
DB_USER=postgres
DB_PASSWORD=postgres123

# Zerodha Configuration
# Note: Zerodha connection is configured via pyScript/kite-curl-request.txt
# Make sure this file contains a valid curl request from Zerodha Kite

# Data Configuration
FETCH_INTERVAL_MS=1000
MAX_TICK_AGE_MS=5000
DATA_RETENTION_DAYS=30

# Logging
LOG_LEVEL=info